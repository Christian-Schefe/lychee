module various_test;
module std;

import std::arrays::IntArray;
import std::arrays::create@IntArray;
import std::arrays::fill@IntArray;
import std::arrays::print@IntArray;
import std::arrays::set@IntArray;

int main() {
    let var int_arr = create@IntArray(500);
    int_arr.fill(-8);
    int_arr.set(0, 1);
    int_arr.print();
    print@IntArray(int_arr);
    (int_arr.data[0]).test();
    0
}

impl int {
    unit test(int a) {
        std::arrays::write_num(a * 88);
    }
}
