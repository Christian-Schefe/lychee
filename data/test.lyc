int main() {
    int pointed = 5;
    int test = 3;
    &int pointer = &pointed;
    write_alphabet(&test);
    return (int)('a' == 'a');
}

unit pointer_test(&int pointer) {
    *pointer = 125;
}

unit write_alphabet(&int counter) {
    for (*counter = 0; *counter < 26; ++*counter) {
        writechar((char)(*counter + (int)'a'));
    }
}