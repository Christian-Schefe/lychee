module arrays;

int main() {
    for (let int i = 0; i < 10; i++) {
        let int i = 3;
        if (i % 2 == 0) {
            write_char('0');
        } else {
            write_char('1');
            continue;
        }
    };
    i = random() as int;
    let var arr = arrays::newIntArray(5);
    arr = arrays::newIntArray(5);
    arrays::fill(&arr, -5);
    arrays::printArray(&arr);
    let int k = 1 + 1 +1+1+1+1+1+1+1;
    i
}
